export interface IPlanCategory {
  categoryId: number;
  categoryName: string;
  activeSwitch?: string | null;
  cerateDate?: string;
  updateDate?: string;
  createdBy?: string;
  updateBy?: string;
}

// categoryId	[...]
// categoryName	[...]
// activeSwitch	[...]
// cerateDate	[...]
// updateDate	[...]
// createdBy	[...]
// updateBy	[...]
